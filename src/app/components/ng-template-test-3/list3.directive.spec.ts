import { List3Directive } from './list3.directive';

describe('List3Directive', () => {
  it('should create an instance', () => {
    const directive = new List3Directive();
    expect(directive).toBeTruthy();
  });
});
