import { Card3Directive } from './card3.directive';

describe('Card3Directive', () => {
  it('should create an instance', () => {
    const directive = new Card3Directive();
    expect(directive).toBeTruthy();
  });
});
